Options -MultiViews
DirectoryIndex index.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  RewriteCond %{HTTPS} !=on [OR]
  RewriteCond %{HTTP_HOST} ^www\.abhinaykhalatkar\.de$ [NC]
  RewriteRule ^ https://abhinaykhalatkar.de%{REQUEST_URI} [R=301,L]

  RewriteRule ^$ /en/ [R=301,L]

  RewriteRule ^index\.html$ /en/ [R=301,L]
  RewriteRule ^(.+)/index\.html$ /$1/ [R=301,L]

  # Trailing slash for locale-prefixed paths (pattern-based, no filesystem check)
  RewriteCond %{REQUEST_URI} !\.
  RewriteCond %{REQUEST_URI} !/$
  RewriteRule ^(en|de)(/.*)$ /$1$2/ [R=301,L]

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule . /index.html [L]
</IfModule>

<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/html "access plus 0 seconds"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType image/svg+xml "access plus 1 month"
  ExpiresByType image/x-icon "access plus 1 month"
  ExpiresByType image/png "access plus 1 month"
  ExpiresByType image/jpeg "access plus 1 month"
  ExpiresByType font/woff2 "access plus 1 month"
  ExpiresByType application/pdf "access plus 1 month"
</IfModule>
