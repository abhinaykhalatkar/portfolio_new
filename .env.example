# Keep "/" when your domain document root points directly to the deployed app folder.
# Only change this if URLs must include a sub-path prefix.
VITE_BASE_PATH=/
# Optional: override GitHub username used for project repository fetching.
VITE_GITHUB_USERNAME=abhinaykhalatkar
# Optional: override Home timeline feed URL.
VITE_TIMELINE_SOURCE_URL=/data/linkedin-timeline.json
